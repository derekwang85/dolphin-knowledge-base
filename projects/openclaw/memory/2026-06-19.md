
## 00:45 - TradeOMS 6h 审计巡检

- 审计脚本执行成功，报告已推送给 Derek
- 修复了脚本 bug：`high_fail` 变量未初始化导致 `UnboundLocalError`
- 关键数据：312测试全绿 PASS，Reasonix 0实例
- 内存读取异常（free -h 输出为空），需后续排查
## 06:42 — 编译重启经验沉淀

### 今日修复记录
- **MySQL(Docker)**: ✅ Up 2天, 124 tables, Flyway v88
- **后端重启**: 编译失败(import缺失+类型不匹配) → Spring启动失败(条件Bean链式爆炸)
- **修复**: 14 个文件改动, 含 import 补全、类型修正、@ConditionalOnProperty 移除
- **构建门禁**: scripts/pre-compile-check.sh (stale import + conditional bean 检测)

### 关键教训
1. DTO 重构后 import 不会自动更新 → stale import → "找不到符号"
2. @ConditionalOnProperty 只放在 Client 层但下游 Service/Controller 无条件 → 启动时链式崩溃
3. 经验已写入: docs/lessons/compile-bootstrap-lessons.md + 纳入 build gate (pre-commit + CI)

## 06:45 — 经验沉淀完成
- docs/lessons/compile-bootstrap-lessons.md → TradeOMS 项目知识库
- dereknbstartup skill → 新增预检步骤 + 常见陷阱说明
- MEMORY.md → 已汇总关键模式

## 14:00~18:00 — DereInside Phase 1-3 全量完成 + 基础设施搭建

### 项目状态
- **DereInside** 是一个本地优先 AI 知识系统，从 gbrain 的教训中重写
- **Phase 0** ✅: gbrain → derekinside 数据迁移（12 wings, 44 rooms, 555 pages, 2651 chunks）
- **Phase 1** ✅: 分层索引 Wing/Room/Drawer + 混合搜索（向量+FTS+RRF）+ LLM 重排序
- **Phase 2** ✅: 知识图（687 entities, 1427 links）+ 正则/LLM 混合实体提取 + PageRank 传播重排序
- **Phase 3** ✅: MCP 服务器（6 tools, JSON-RPC 2.0 over stdio）+ FastAPI HTTP 桥（6 端点）+ Agent 隔离命名空间

### GitHub 项目管理
- derekhubproj skill 发布 — GitHub 驱动的项目管理规约（Issue/Phase/PR/WBS 映射 + 校验门禁）
- Issue 模板、PR 模板、labels 已部署
- Issues #1-#13 全部追踪并关闭，含 #8-#11 集中修复
- CI pipeline 已创建 (`.github/workflows/ci.yml`)，需 Derek GitHub 网页配置 branch protection

### 硬件评估
- 实测瓶颈：bge-m3 embedding 2.3s/条（CPU 天花板），混合搜索 ~8ms，图传播 ~2ms
- EmbeddingCache LRU 实现：命中 4.6ms vs 未命中 3322ms → **720x 加速**
- 三级配置建议写入 README：Personal（4核/8GB）、Team（8核/32GB/GPU）、Production（16核/64GB/GPU）
- 存储公式：1 文件 ≈ 5 chunks ≈ 2 MB pgvector

### 连接池重构
- `psycopg.connect` → `psycopg_pool.ConnectionPool` (min=1, max=5)
- 11 个内部方法全部更新为 `with self.cursor() as cur:` 模式
- 解决了单连接设计导致的高并发问题

### 磁盘迁移
- Ollama 模型（14.1GB）从 Docker volume → `/media/cbnb/_opdata/ollama/`（500GB 媒体盘）
- derekinside 存储路径指向 `_opdata/derekinside/`
- 结果：系统盘腾出 21GB，媒体盘占用 15GB / 467GB

### 命名由来
- DereInside = 拉丁语 `dare`（给予）→ `dere` + `inside` = **"从内部给予知识"**
- 替代原版 De(rek) + Inside 的硬凑解释

### 系统部署
- HTTP bridge 以 systemd user service 运行在 port 18890
- `~/.config/systemd/user/derekinside-http.service`
- 开机自启，健康检查 `GET /health`

### 与竞品对比
- 独有优势：混合 5 层检索（向量+FTS+RRF+时间+图）、MCP 协议、Agent 隔离
- 主要差距：无前端可视化、无插件生态、无 Benchmark
